@model IEnumerable<kisiselWebsitem.Models.Model.Blog>
@using kisiselWebsitem.Helpers;
@foreach (var item in Model)
{
    <li>
        <img src="@item.ResimURL" class="pull-left" alt="@item.Baslik" />
        <h6><a href="/BlogPost/@SeoHelpers.ToSeoUrl(item.Baslik).ToLower()-@item.BlogId">@item.Baslik</a></h6>
        <p>
            @Html.Raw(item.Icerik.Substring(0, 150))
        </p>
    </li>
}

